SMSChecker Project is the code.
SMSCheckerService is the installer.

Rebuild the code (to release)
Rebuild the installer (to release)

Open the folder in explorer, go to the release folder, copy to the server, you may have to stop the service, uninstall the program, and then run the installation